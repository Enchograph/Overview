{
  "name": "overview",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.3",
  "scripts": {
    "client:doctor": "flutter doctor",
    "client:analyze": "flutter analyze",
    "client:test": "flutter test",
    "api:dev": "corepack pnpm --filter @overview/api dev",
    "api:start": "corepack pnpm --filter @overview/api start",
    "api:test": "corepack pnpm --filter @overview/api test",
    "api:lint": "corepack pnpm --filter @overview/api lint",
    "api:format": "corepack pnpm --filter @overview/api format",
    "api:typecheck": "corepack pnpm --filter @overview/api typecheck",
    "api:build": "corepack pnpm --filter @overview/api build",
    "shared:typecheck": "corepack pnpm --filter @overview/shared typecheck",
    "shared:build": "corepack pnpm --filter @overview/shared build",
    "shared:check": "npm run shared:build && node --experimental-strip-types scripts/check-shared.ts",
    "test": "npm run api:test",
    "check": "npm run api:lint && npm run api:typecheck && npm run api:test",
    "build": "npm run api:build"
  }
}
