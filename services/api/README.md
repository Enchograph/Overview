# api

Node.js + TypeScript 后端 API 服务目录。

当前状态：已初始化零依赖 TypeScript 最小服务骨架。

## 当前内容

- `src/index.ts`：服务入口
- `src/server.ts`：HTTP server 与 `/health` 路由
- `test/health.test.ts`：健康检查测试
- `package.json` / `tsconfig.json`：服务脚本与 TS 配置

## 本地命令

- `npm run api:start`
- `npm run api:test`
